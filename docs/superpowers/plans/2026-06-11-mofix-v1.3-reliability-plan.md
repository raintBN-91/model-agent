# MoFix v1.3 可靠性演进 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在零改动 v1.2 代码的前提下，创建 v1.3 目录并实施测试体系 + 增量加固

**Architecture:** v1.3 = 复制 v1.2 全部文件 + 新增 tests/ 测试目录 + 在 app/ 各文件中叠加增量加固（不改逻辑只加防护）

**Tech Stack:** pytest 7.x, pytest-asyncio, pytest-cov, pytest-mock, ruff, GitHub Actions

---

## File Structure

### 新建文件
```
v1.3/
├── tests/
│   ├── conftest.py              # 全局 fixtures
│   ├── conftest_local.py        # 本地专用 fixtures (真实 LLM)
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── test_exceptions.py
│   │   └── test_llm_factory.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── test_chat.py
│   │   └── test_health.py
│   ├── services/
│   │   ├── __init__.py
│   │   └── test_chat_service.py
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── test_claude_tools.py
│   │   ├── test_doc_tools.py
│   │   ├── test_search_tools.py
│   │   ├── test_verify_tools.py
│   │   └── test_optimizer_tools.py
│   ├── workflow/
│   │   ├── __init__.py
│   │   ├── test_planner.py
│   │   ├── test_executor.py
│   │   └── test_brainstorming.py
│   └── hermes/
│       ├── __init__.py
│       ├── test_store.py
│       └── test_memory.py
├── pytest.ini
├── requirements-dev.txt
└── .github/workflows/ci.yml
```

### 被修改的文件（增量加固 C1-C4）
```
v1.3/app/
├── tools/
│   ├── doc_tools.py             # C1: 超时常量提取
│   ├── claude_tools.py          # C1: asyncio.wait_for 兜底
│   └── workflow/mcp_integration.py  # C1: _discover_tools 超时
├── core/
│   └── llm_factory.py           # C1: retry_llm_call 装饰器
├── models/
│   └── chat.py                  # C2: Pydantic 校验加强
├── services/
│   └── chat_service.py          # C3: 日志补全, C4: 错误格式统一
├── api/
│   └── chat.py                  # C3: 请求入口日志 + trace_id
└── workflow/
    └── planner.py               # C1: _call_llm 先重试再降级 + C3: 日志
```

---

### Phase 1: 基础框架

#### Task 1: 创建 v1.3 目录并复制 v1.2

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3` (directory)

- [ ] **Step 1: 创建 v1.3 目录并复制所有 v1.2 文件**

```bash
# 从 v1.2 复制全部文件到 v1.3
Copy-Item -Path "F:\7-workspace\mofix-new\mofix-v1.2\*" -Destination "F:\7-workspace\mofix-new\mofix-v1.3" -Recurse -Force
# 排除 .codegraph 索引和 __pycache__
Remove-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\.codegraph" -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Path "F:\7-workspace\mofix-new\mofix-v1.3" -Filter "__pycache__" -Recurse -Directory | Remove-Item -Recurse -Force
```

- [ ] **Step 2: 创建 tests/ 目录结构**

```bash
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\core" -ItemType Directory -Force
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\api" -ItemType Directory -Force
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\services" -ItemType Directory -Force
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\tools" -ItemType Directory -Force
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\workflow" -ItemType Directory -Force
New-Item -Path "F:\7-workspace\mofix-new\mofix-v1.3\tests\hermes" -ItemType Directory -Force
```

- [ ] **Step 3: 创建 tests/__init__.py 和各级 __init__.py**

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\__init__.py`:
```python
"""MoFix v1.3 测试套件。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\core\__init__.py`:
```python
"""核心模块测试。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\api\__init__.py`:
```python
"""API 端点测试。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\services\__init__.py`:
```python
"""服务层测试。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\tools\__init__.py`:
```python
"""工具模块测试。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\workflow\__init__.py`:
```python
"""工作流引擎测试。"""
```

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\hermes\__init__.py`:
```python
"""Hermes 引擎测试。"""
```

- [ ] **Step 4: 创建 pytest.ini**

Create `F:\7-workspace\mofix-new\mofix-v1.3\pytest.ini`:
```ini
[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: 需要实际 LLM 或外部依赖的测试
    integration: 集成测试（需要外部服务）
    isolated: 纯单元测试（默认，可离线运行）
pythonpath = .
```

- [ ] **Step 5: 创建 requirements-dev.txt**

Create `F:\7-workspace\mofix-new\mofix-v1.3\requirements-dev.txt`:
```
-r requirements.txt
pytest>=7.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
ruff>=0.1.0
```

- [ ] **Step 6: 创建 tests/conftest.py（全局 fixtures）**

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\conftest.py`:
```python
"""全局 fixtures — 所有测试共享的 mock 和工具函数。"""

from __future__ import annotations

from collections.abc import AsyncGenerator, Generator
from unittest.mock import AsyncMock, MagicMock, patch

import pytest
from pytest_mock import MockerFixture


@pytest.fixture(autouse=True)
def _mock_lts_logger(mocker: MockerFixture) -> MagicMock:
    """自动 mock lts_logger，防止测试中产生真实日志上报。"""
    return mocker.patch("lts_logger.log", return_value=None)


@pytest.fixture(autouse=True)
def _mock_subprocess_run(mocker: MockerFixture) -> MagicMock:
    """自动 mock subprocess.run — 防止意外执行系统命令。"""
    mock = MagicMock()
    mock.return_value.returncode = 0
    mock.return_value.stdout = ""
    mock.return_value.stderr = ""
    return mocker.patch("subprocess.run", mock)


@pytest.fixture
def mock_claude_agent_sdk(mocker: MockerFixture) -> AsyncGenerator[AsyncMock, None]:
    """Mock claude_agent_sdk.query — 返回异步迭代器。"""
    async def _fake_query(*args, **kwargs):
        class FakeResultMessage:
            def __init__(self):
                self.result = "mocked claude result"

        yield FakeResultMessage()

    mock = AsyncMock(side_effect=_fake_query())
    mocker.patch("claude_agent_sdk.query", return_value=mock)
    yield mock


@pytest.fixture
def mock_llm_invoke(mocker: MockerFixture) -> MagicMock:
    """Mock ChatOpenAI.invoke — 返回结构化 JSON 响应。"""
    mock_response = MagicMock()
    mock_response.content = '{"type": "question", "question": {"text": "test", "options": []}}'
    return mocker.patch("langchain_openai.ChatOpenAI.invoke", return_value=mock_response)


@pytest.fixture
def tmp_data_dir(tmp_path):
    """创建临时数据目录，模拟 ~/.mofix 结构。"""
    d = tmp_path / ".mofix"
    d.mkdir()
    return d
```

- [ ] **Step 7: 创建 tests/conftest_local.py**

Create `F:\7-workspace\mofix-new\mofix-v1.3\tests\conftest_local.py`:
```python
"""本地专用 fixtures — 需要真实 LLM/外部服务。仅在本地手动运行，CI 中跳过。"""

from __future__ import annotations

import pytest


@pytest.fixture
def real_llm():
    """返回一个连接到真实 API 的 ChatOpenAI 实例。
    
    使用方式：在测试函数参数中加入 `real_llm`，
    然后通过 `pytest -m integration` 运行。
    """
    from app.core.llm_factory import build_llm
    return build_llm(streaming=False)


def pytest_configure(config):
    config.addinivalue_line("markers", "integration: 需要真实 LLM/外部服务的集成测试")
```

- [ ] **Step 8: 创建 .github/workflows/ci.yml**

Create `F:\7-workspace\mofix-new\mofix-v1.3\.github\workflows\ci.yml`:
```yaml
name: CI

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install ruff
      - name: Lint
        run: ruff check app/ tests/

  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov pytest-mock
      - name: Run unit tests
        run: |
          pytest tests/ -m "not integration" --cov=app --cov-report=term-missing --cov-fail-under=60

  integration-test:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio
      - name: Run integration tests
        run: pytest tests/ -m integration
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
```

- [ ] **Step 9: 验证 pytest 可运行**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pip install -r requirements-dev.txt
pytest --collect-only
```
Expected: 所有测试文件被收集（初始为 0 个测试），无报错。

- [ ] **Step 10: 提交 Phase 1**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
git init
git add .
git commit -m "feat: init v1.3 from v1.2 with test infrastructure"
```

---

### Phase 2: P0 测试

#### Task 2: test_exceptions.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\core\test_exceptions.py`

- [ ] **Step 1: Write the test**

```python
"""Test MoFix exception hierarchy — status_code, openai_type, openai_code mappings."""

from __future__ import annotations

import pytest

from app.core.exceptions import (
    CommandNotFoundError,
    InvalidRequestError,
    LLMAuthError,
    LLMNotFoundError,
    LLMQuotaError,
    LLMThinkingError,
    MoFixException,
    PendingConfirmationError,
)


class TestMoFixExceptions:
    """验证每个异常类的 status_code/openai_type/openai_code 属性。"""

    @pytest.mark.parametrize("exc_cls,expected", [
        (MoFixException, {"status_code": 500, "openai_type": "api_error", "openai_code": "internal_error"}),
        (LLMAuthError, {"status_code": 401, "openai_type": "invalid_request_error", "openai_code": "invalid_api_key"}),
        (LLMNotFoundError, {"status_code": 404, "openai_type": "invalid_request_error", "openai_code": "not_found"}),
        (LLMQuotaError, {"status_code": 429, "openai_type": "rate_limit_error", "openai_code": "rate_limit_exceeded"}),
        (LLMThinkingError, {"status_code": 400, "openai_type": "invalid_request_error", "openai_code": "bad_request"}),
        (InvalidRequestError, {"status_code": 400, "openai_type": "invalid_request_error", "openai_code": "invalid_parameter"}),
        (CommandNotFoundError, {"status_code": 400, "openai_type": "invalid_request_error", "openai_code": "invalid_command"}),
        (PendingConfirmationError, {"status_code": 400, "openai_type": "invalid_request_error", "openai_code": "pending_confirmation"}),
    ])
    def test_exception_properties(self, exc_cls, expected):
        exc = exc_cls("test message")
        assert exc.status_code == expected["status_code"]
        assert exc.openai_type == expected["openai_type"]
        assert exc.openai_code == expected["openai_code"]

    def test_exception_can_contain_message(self):
        exc = MoFixException("custom message")
        assert str(exc) == "custom message"

    def test_exception_inheritance(self):
        assert issubclass(LLMAuthError, MoFixException)
        assert issubclass(LLMNotFoundError, MoFixException)
        assert issubclass(LLMQuotaError, MoFixException)
        assert issubclass(LLMThinkingError, MoFixException)
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/core/test_exceptions.py -v
```
Expected: 10 tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/core/test_exceptions.py tests/core/__init__.py
git commit -m "test: add exception hierarchy tests (P0)"
```

#### Task 3: test_llm_factory.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\core\test_llm_factory.py`

- [ ] **Step 1: Write the test**

```python
"""Test LLM factory — build_llm, build_workflow_llm, handle_api_error."""

from __future__ import annotations

import pytest
from langchain_openai import ChatOpenAI

from app.core.exceptions import (
    LLMAuthError,
    LLMNotFoundError,
    LLMQuotaError,
    LLMThinkingError,
)
from app.core.llm_factory import (
    _normalize_base_url,
    build_llm,
    build_workflow_llm,
    handle_api_error,
)


class TestNormalizeBaseUrl:
    def test_already_has_v1(self):
        assert _normalize_base_url("https://api.example.com/v1") == "https://api.example.com/v1"

    def test_no_v1_appended(self):
        assert _normalize_base_url("https://api.example.com") == "https://api.example.com/v1"

    def test_strips_trailing_slash(self):
        assert _normalize_base_url("https://api.example.com/") == "https://api.example.com/v1"

    def test_empty_base_url(self):
        assert _normalize_base_url("") == "/v1"


class TestBuildLlm:
    def test_build_llm_success(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "test-key")
        mocker.patch("app.config.settings.openai_base_url", "https://api.example.com")
        mocker.patch("app.config.settings.openai_model", "test-model")
        llm = build_llm(streaming=False)
        assert isinstance(llm, ChatOpenAI)
        assert llm.model == "test-model"

    def test_build_llm_missing_api_key(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "")
        mocker.patch("app.config.settings.moonshot_api_key", "")
        with pytest.raises(LLMAuthError, match="OPENAI_API_KEY"):
            build_llm()

    def test_build_llm_with_moonshot_fallback(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "")
        mocker.patch("app.config.settings.moonshot_api_key", "moonshot-key")
        mocker.patch("app.config.settings.openai_base_url", "")
        mocker.patch("app.config.settings.moonshot_base_url", "")
        mocker.patch("app.config.settings.openai_model", "moonshot-model")
        llm = build_llm(streaming=False)
        assert isinstance(llm, ChatOpenAI)

    def test_kimi_thinking_disabled(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "test-key")
        mocker.patch("app.config.settings.openai_base_url", "https://api.example.com")
        mocker.patch("app.config.settings.openai_model", "kimi-k2.5")
        llm = build_llm(streaming=False)
        assert llm.model == "kimi-k2.5"
        # extra_body should have thinking disabled for kimi models
        assert llm.extra_body is not None
        assert llm.extra_body.get("thinking", {}).get("type") == "disabled"


class TestBuildWorkflowLlm:
    def test_workflow_llm_uses_dedicated_model(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "test-key")
        mocker.patch("app.config.settings.openai_base_url", "https://api.example.com")
        mocker.patch("app.config.settings.workflow_llm_model", "fast-model")
        mocker.patch("app.config.settings.openai_model", "slow-model")
        llm = build_workflow_llm()
        assert llm.model == "fast-model"

    def test_workflow_llm_falls_back_to_openai_model(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "test-key")
        mocker.patch("app.config.settings.openai_base_url", "https://api.example.com")
        mocker.patch("app.config.settings.workflow_llm_model", "")
        mocker.patch("app.config.settings.openai_model", "default-model")
        llm = build_workflow_llm()
        assert llm.model == "default-model"

    def test_workflow_llm_low_temperature(self, mocker):
        mocker.patch("app.config.settings.openai_api_key", "test-key")
        mocker.patch("app.config.settings.openai_base_url", "https://api.example.com")
        mocker.patch("app.config.settings.workflow_llm_model", "planner")
        mocker.patch("app.config.settings.openai_model", "default")
        llm = build_workflow_llm()
        assert llm.temperature == 0.1


class TestHandleApiError:
    def test_401_raises_auth_error(self):
        with pytest.raises(LLMAuthError):
            handle_api_error(Exception("401 Unauthorized"))

    def test_404_raises_not_found_error(self):
        with pytest.raises(LLMNotFoundError):
            handle_api_error(Exception("404 Not Found"))

    def test_429_raises_quota_error(self):
        with pytest.raises(LLMQuotaError):
            handle_api_error(Exception("429 Too Many Requests"))

    def test_insufficient_balance_raises_quota_error(self):
        with pytest.raises(LLMQuotaError):
            handle_api_error(Exception("insufficient balance"))

    def test_thinking_conflict_raises_thinking_error(self):
        with pytest.raises(LLMThinkingError):
            handle_api_error(Exception("reasoning_content not supported"))

    def test_unknown_error_re_raised(self):
        with pytest.raises(ValueError, match="something else"):
            handle_api_error(ValueError("something else"))
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/core/test_llm_factory.py -v
```
Expected: 15+ tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/core/test_llm_factory.py
git commit -m "test: add LLM factory tests (P0)"
```

#### Task 4: test_api.py — health + chat

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\api\test_health.py`
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\api\test_chat.py`

- [ ] **Step 1: Write test_health.py**

```python
"""Test health check endpoint."""

from __future__ import annotations

from fastapi.testclient import TestClient

from app.main import app

client = TestClient(app)


class TestHealth:
    def test_health_returns_200(self):
        resp = client.get("/health")
        assert resp.status_code == 200

    def test_health_returns_json(self):
        resp = client.get("/health")
        assert resp.headers["content-type"] == "application/json"

    def test_health_has_status_ok(self):
        resp = client.get("/health")
        data = resp.json()
        assert data.get("status") == "ok"
```

- [ ] **Step 2: Write test_chat.py**

```python
"""Test chat completion endpoint — request validation."""

from __future__ import annotations

import pytest
from pydantic import ValidationError

from app.models.chat import ChatCompletionRequest, ChatMessage


class TestChatMessage:
    def test_valid_message(self):
        msg = ChatMessage(role="user", content="hello")
        assert msg.role == "user"
        assert msg.content == "hello"

    def test_message_with_tool_fields(self):
        msg = ChatMessage(role="tool", content="result", name="search", tool_call_id="call_1")
        assert msg.tool_call_id == "call_1"

    def test_invalid_role_raises(self):
        with pytest.raises(ValidationError):
            ChatMessage(role="invalid_role", content="text")


class TestChatCompletionRequest:
    def test_valid_request(self):
        req = ChatCompletionRequest(messages=[ChatMessage(role="user", content="hi")])
        assert req.stream is True  # default
        assert req.messages[0].content == "hi"

    def test_empty_messages_raises(self):
        with pytest.raises(ValidationError, match="不能为空"):
            ChatCompletionRequest(messages=[])

    def test_last_message_must_be_user(self):
        with pytest.raises(ValidationError, match="必须是 user"):
            ChatCompletionRequest(messages=[
                ChatMessage(role="user", content="first"),
                ChatMessage(role="assistant", content="last"),
            ])

    def test_system_message_allowed_in_middle(self):
        req = ChatCompletionRequest(messages=[
            ChatMessage(role="system", content="you are helpful"),
            ChatMessage(role="user", content="hi"),
        ])
        assert len(req.messages) == 2

    def test_optional_fields(self):
        req = ChatCompletionRequest(
            messages=[ChatMessage(role="user", content="hi")],
            stream=False,
            temperature=0.5,
            max_tokens=1000,
            c_id="test-session",
        )
        assert req.stream is False
        assert req.temperature == 0.5
        assert req.max_tokens == 1000
        assert req.c_id == "test-session"
```

- [ ] **Step 3: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/api/ -v
```
Expected: Tests pass.

- [ ] **Step 4: Commit**

```bash
git add tests/api/
git commit -m "test: add API endpoint validation tests (P0)"
```

#### Task 5: test_commands.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\test_commands.py`

- [ ] **Step 1: Write the test**

```python
"""Test command registry — registration, lookup, system prompt."""

from __future__ import annotations

from commands.registry import Command, CommandRegistry, TriggerMode


class TestCommandRegistry:
    def test_register_and_get(self):
        reg = CommandRegistry()
        cmd = Command(name="test", description="A test command", trigger_modes=[TriggerMode.USER_TRIGGER])
        reg.register(cmd)
        assert reg.get("test") is cmd

    def test_get_unknown_returns_none(self):
        reg = CommandRegistry()
        assert reg.get("nonexistent") is None

    def test_get_case_sensitive(self):
        reg = CommandRegistry()
        cmd = Command(name="TestCmd", description="case test", trigger_modes=[TriggerMode.USER_TRIGGER])
        reg.register(cmd)
        assert reg.get("TestCmd") is cmd
        assert reg.get("testcmd") is None

    def test_build_system_prompt_addon_with_commands(self):
        reg = CommandRegistry()
        reg.register(Command(
            name="verify",
            description="验证模型适配",
            trigger_modes=[TriggerMode.USER_TRIGGER, TriggerMode.LLM_RECOMMEND],
            example="/verify 验证 Qwen3-8B",
        ))
        reg.register(Command(
            name="search",
            description="搜索模型",
            trigger_modes=[TriggerMode.LLM_AUTO, TriggerMode.LLM_RECOMMEND],
        ))
        prompt = reg.build_system_prompt_addon()
        assert "/verify" in prompt
        assert "/search" in prompt
        assert "USER_TRIGGER" in prompt or "用户" in prompt
        assert "LLM_AUTO" in prompt or "自动" in prompt
        assert "Qwen3-8B" in prompt

    def test_build_system_prompt_addon_empty(self):
        reg = CommandRegistry()
        prompt = reg.build_system_prompt_addon()
        assert prompt == ""

    def test_multiple_registrations(self):
        reg = CommandRegistry()
        for i in range(5):
            reg.register(Command(name=f"cmd{i}", description=f"Command {i}", trigger_modes=[TriggerMode.USER_TRIGGER]))
        for i in range(5):
            assert reg.get(f"cmd{i}") is not None

    def test_trigger_mode_membership(self):
        cmd = Command(name="x", description="x", trigger_modes=[TriggerMode.USER_TRIGGER, TriggerMode.LLM_RECOMMEND])
        assert TriggerMode.USER_TRIGGER in cmd.trigger_modes
        assert TriggerMode.LLM_RECOMMEND in cmd.trigger_modes
        assert TriggerMode.LLM_AUTO not in cmd.trigger_modes
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/test_commands.py -v
```
Expected: 8+ tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/test_commands.py
git commit -m "test: add command registry tests (P0)"
```

---

### Phase 3: P1 测试

#### Task 6: test_agent_engine.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\core\test_agent_engine.py`

- [ ] **Step 1: Write the test**

```python
"""Test agent engine — build_agent, prompt assembly, skills registry."""

from __future__ import annotations

from unittest.mock import MagicMock, patch

import pytest

from app.core.agent_engine import (
    _build_skill_recommendation_prompt,
    _load_skills_registry,
    build_agent,
)
from commands.registry import Command, CommandRegistry, TriggerMode


class TestLoadSkillsRegistry:
    def test_skills_dir_not_found_returns_empty(self, mocker):
        mocker.patch("pathlib.Path.is_dir", return_value=False)
        assert _load_skills_registry() == {}

    def test_parses_skill_frontmatter(self, mocker, tmp_path):
        skills_dir = tmp_path / "skills"
        skills_dir.mkdir()
        skill_md = skills_dir / "test-agent" / "SKILL.md"
        skill_md.parent.mkdir()
        skill_md.write_text("---\nname: test-agent\ndescription: A test skill\n---\n\nContent here")
        
        mocker.patch("pathlib.Path.__init__", return_value=None)
        mocker.patch("pathlib.Path.resolve", return_value=tmp_path)
        # We need to mock skill_dir resolution
        mocker.patch("app.core.agent_engine.Path", return_value=tmp_path)
        mocker.patch("pathlib.Path.rglob", return_value=[skill_md])
        mocker.patch("pathlib.Path.is_dir", return_value=True)
        
        # Since the Path mocking is complex for the real code, let's use integration approach
        # This test needs refinement - for now, test the prompt builder
        pass

    def test_rglob_finds_no_skills(self, mocker):
        mocker.patch("pathlib.Path.is_dir", return_value=True)
        mocker.patch("pathlib.Path.rglob", return_value=[])
        assert _load_skills_registry() == {}


class TestBuildSkillRecommendationPrompt:
    def test_empty_registry_returns_empty(self, mocker):
        mocker.patch("app.core.agent_engine._load_skills_registry", return_value={})
        assert _build_skill_recommendation_prompt() == ""

    def test_with_skills_includes_names(self, mocker):
        mocker.patch("app.core.agent_engine._load_skills_registry", return_value={
            "verify-agent": "验证模型适配",
            "adapt-agent": "执行模型适配",
        })
        prompt = _build_skill_recommendation_prompt()
        assert "verify-agent" in prompt
        assert "adapt-agent" in prompt
        assert "/claude" in prompt


class TestBuildAgent:
    def test_build_agent_returns_compiled_graph(self, mocker):
        mocker.patch("app.core.agent_engine.build_llm", return_value=MagicMock())
        mocker.patch("app.core.agent_engine.get_all_tools", return_value=[])
        mocker.patch("app.core.agent_engine.get_tool_names", return_value=[])
        mocker.patch("app.core.agent_engine._build_skill_recommendation_prompt", return_value="")
        mocker.patch("app.core.agent_engine._build_command_prompt", return_value="")

        agent = build_agent()
        assert agent is not None

    def test_build_agent_with_command_registry(self, mocker):
        mocker.patch("app.core.agent_engine.build_llm", return_value=MagicMock())
        mocker.patch("app.core.agent_engine.get_all_tools", return_value=[])
        mocker.patch("app.core.agent_engine.get_tool_names", return_value=[])
        mocker.patch("app.core.agent_engine._build_skill_recommendation_prompt", return_value="")
        mocker.patch("app.core.agent_engine._build_command_prompt", return_value="\n/verify 验证")

        reg = CommandRegistry()
        reg.register(Command(name="verify", description="验证", trigger_modes=[TriggerMode.USER_TRIGGER]))
        agent = build_agent(cmd_registry=reg)
        assert agent is not None

    def test_user_system_prompt_appended(self, mocker):
        mocker.patch("app.core.agent_engine.build_llm", return_value=MagicMock())
        mocker.patch("app.core.agent_engine.get_all_tools", return_value=[])
        mocker.patch("app.core.agent_engine.get_tool_names", return_value=[])
        mocker.patch("app.core.agent_engine._build_skill_recommendation_prompt", return_value="")
        mocker.patch("app.core.agent_engine._build_command_prompt", return_value="")

        agent = build_agent(user_system_prompt="custom prompt here")
        assert agent is not None
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/core/test_agent_engine.py -v
```
Expected: Tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/core/test_agent_engine.py
git commit -m "test: add agent engine tests (P1)"
```

#### Task 7: test_chat_service.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\services\test_chat_service.py`

- [ ] **Step 1: Write the test**

```python
"""Test chat service — SSE chunks, message conversion, command execution."""

from __future__ import annotations

import json
from unittest.mock import AsyncMock, MagicMock

import pytest

from app.models.chat import ChatCompletionChunk, ChatMessage
from app.services.chat_service import (
    _build_sse_chunk,
    _convert_messages,
    with_heartbeat,
)
from commands.registry import Command, CommandRegistry, TriggerMode


class TestBuildSseChunk:
    def test_basic_chunk_structure(self):
        chunk = _build_sse_chunk("cmpl-123", "test-model", MagicMock(content="hello"))
        assert chunk.startswith("data: ")
        assert chunk.endswith("\n\n")
        data = json.loads(chunk[6:].strip())
        assert data["id"] == "cmpl-123"
        assert data["model"] == "test-model"
        assert data["object"] == "chat.completion.chunk"

    def test_finish_reason_included(self):
        chunk = _build_sse_chunk("cmpl-1", "m", MagicMock(content=""), finish_reason="stop")
        data = json.loads(chunk[6:].strip())
        assert data["choices"][0]["finish_reason"] == "stop"

    def test_requires_confirmation_flag(self):
        chunk = _build_sse_chunk("cmpl-1", "m", MagicMock(content=""), requires_confirmation=True)
        data = json.loads(chunk[6:].strip())
        assert data["choices"][0].get("requires_confirmation") is True

    def test_done_sentinel(self):
        done = "data: [DONE]\n\n"
        assert done.startswith("data: ")
        assert "[DONE]" in done


class TestConvertMessages:
    def test_system_message(self):
        msgs = [ChatMessage(role="system", content="be helpful")]
        converted = _convert_messages(msgs)
        assert len(converted) == 1
        assert converted[0].type == "system"

    def test_user_message(self):
        msgs = [ChatMessage(role="user", content="hello")]
        converted = _convert_messages(msgs)
        assert converted[0].type == "human"

    def test_assistant_message(self):
        msgs = [ChatMessage(role="assistant", content="hi there")]
        converted = _convert_messages(msgs)
        assert converted[0].type == "ai"

    def test_tool_message(self):
        msgs = [ChatMessage(role="tool", content="result", name="search", tool_call_id="call_1")]
        converted = _convert_messages(msgs)
        assert converted[0].type == "tool"

    def test_multiple_messages(self):
        msgs = [
            ChatMessage(role="system", content="system"),
            ChatMessage(role="user", content="user"),
            ChatMessage(role="assistant", content="assistant"),
            ChatMessage(role="tool", content="tool", name="t", tool_call_id="c"),
        ]
        converted = _convert_messages(msgs)
        assert len(converted) == 4


class TestWithHeartbeat:
    @pytest.mark.asyncio
    async def test_heartbeat_injects_pings(self):
        async def slow_source():
            yield "chunk1\n"
            import asyncio
            await asyncio.sleep(0.3)
            yield "chunk2\n"

        results = []
        async for chunk in with_heartbeat(slow_source(), interval=0.05):
            results.append(chunk)
            if len(results) >= 3:
                break

        assert any("PING" in c for c in results)

    @pytest.mark.asyncio
    async def test_passes_through_chunks(self):
        async def source():
            yield "a\n"
            yield "b\n"

        results = [c async for c in with_heartbeat(source(), interval=5.0)]
        assert "a\n" in results
        assert "b\n" in results
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/services/test_chat_service.py -v
```
Expected: Tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/services/
git commit -m "test: add chat service tests (P1)"
```

#### Task 8: test_planner.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\workflow\test_planner.py`

- [ ] **Step 1: Write the test**

```python
"""Test workflow planner — plan generation, LLM response parsing, DAG validation."""

from __future__ import annotations

import json
from unittest.mock import MagicMock

import pytest

from app.tools.workflow.planner import DynamicPlanner
from app.tools.workflow.types import StepType, WorkflowPlan


class TestParseResponse:
    def test_direct_json_parse(self):
        planner = DynamicPlanner(llm=MagicMock())
        raw = '{"goal": "test", "steps": []}'
        result = planner._parse_response(raw)
        assert result is not None
        assert result["goal"] == "test"

    def test_markdown_json_code_block(self):
        planner = DynamicPlanner(llm=MagicMock())
        raw = '```json\n{"goal": "test", "steps": []}\n```'
        result = planner._parse_response(raw)
        assert result is not None
        assert result["goal"] == "test"

    def test_json_extracted_from_text(self):
        planner = DynamicPlanner(llm=MagicMock())
        raw = 'Here is the plan: {"goal": "test", "steps": [{"id": "s1", "name": "Step 1"}]}'
        result = planner._parse_response(raw)
        assert result is not None
        assert result["goal"] == "test"
        assert len(result["steps"]) == 1

    def test_invalid_json_returns_none(self):
        planner = DynamicPlanner(llm=MagicMock())
        raw = "not json at all { broken"
        result = planner._parse_response(raw)
        assert result is None


class TestBuildPlan:
    def test_build_plan_from_dict(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan_dict = {
            "goal": "Verify Qwen3-8B",
            "max_concurrency": 2,
            "verify_enabled": True,
            "steps": [
                {"id": "step_1", "name": "Check env", "type": "claude_skill", "skill_name": "verify-agent"},
                {"id": "step_2", "name": "Run tests", "type": "mcp_tool", "depends_on": ["step_1"]},
            ]
        }
        plan = planner._build_plan(plan_dict, "test input")
        assert isinstance(plan, WorkflowPlan)
        assert plan.goal == "Verify Qwen3-8B"
        assert len(plan.steps) == 2
        assert plan.max_concurrency == 2
        assert plan.verify_enabled is True

    def test_step_with_unknown_type_defaults_to_claude_skill(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan_dict = {
            "goal": "test",
            "steps": [{"id": "s1", "name": "S1", "type": "unknown_type"}]
        }
        plan = planner._build_plan(plan_dict, "test")
        assert plan.steps[0].type == StepType.CLAUDE_SKILL

    def test_parallel_steps_parsed_correctly(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan_dict = {
            "goal": "test",
            "steps": [{
                "id": "p1",
                "name": "Parallel",
                "type": "parallel",
                "parallel_steps": [
                    {"id": "ps1", "name": "Sub 1", "type": "claude_skill"},
                    {"id": "ps2", "name": "Sub 2", "type": "mcp_tool"},
                ]
            }]
        }
        plan = planner._build_plan(plan_dict, "test")
        assert plan.steps[0].parallel_steps is not None
        assert len(plan.steps[0].parallel_steps) == 2


class TestValidatePlan:
    def test_cycle_detection_removes_all_deps(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan_dict = {
            "goal": "test",
            "steps": [
                {"id": "a", "name": "A", "type": "claude_skill", "depends_on": ["b"]},
                {"id": "b", "name": "B", "type": "claude_skill", "depends_on": ["a"]},
            ]
        }
        plan = planner._build_plan(plan_dict, "test")
        planner._validate_plan(plan)
        # After cycle detection, dependencies should be cleared to serial
        # a has no dep, b depends on a
        assert plan.steps[0].depends_on == [] or plan.steps[0].depends_on == []
        # The cycle fix makes them serial: b depends on a
        assert plan.steps[1].depends_on == [plan.steps[0].id]

    def test_missing_dep_autofixed(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan_dict = {
            "goal": "test",
            "steps": [
                {"id": "a", "name": "A", "type": "claude_skill", "depends_on": ["nonexistent"]},
            ]
        }
        plan = planner._build_plan(plan_dict, "test")
        planner._validate_plan(plan)
        assert plan.steps[0].depends_on == []


class TestFallbackPlan:
    def test_fallback_plan_creates_single_step(self):
        planner = DynamicPlanner(llm=MagicMock())
        plan = planner._fallback_plan("user request")
        assert len(plan.steps) == 1
        assert plan.steps[0].type == StepType.CLAUDE_SKILL
        assert plan.steps[0].prompt_template == "user request"
        assert plan.verify_enabled is False
        assert plan.max_concurrency == 1
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/workflow/test_planner.py -v
```
Expected: Tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/workflow/test_planner.py
git commit -m "test: add workflow planner tests (P1)"
```

#### Task 9: test_executor.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\workflow\test_executor.py`

- [ ] **Step 1: Write the test**

```python
"""Test workflow executor — DAG execution, checkpoint, retry."""

from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.tools.workflow.executor import WorkflowExecutor
from app.tools.workflow.types import (
    ExecutionContext,
    StepStatus,
    StepType,
    WorkflowPlan,
    WorkflowStep,
)


class TestWorkflowExecutor:
    @pytest.mark.asyncio
    async def test_execute_empty_plan(self):
        executor = WorkflowExecutor()
        plan = WorkflowPlan(steps=[], intent="test")
        ctx = await executor.execute(plan)
        assert ctx is not None
        assert isinstance(ctx, ExecutionContext)

    @pytest.mark.asyncio
    async def test_execute_single_step(self, mocker):
        executor = WorkflowExecutor()
        step = WorkflowStep(
            id="s1",
            name="Test Step",
            type=StepType.CLAUDE_SKILL,
            prompt_template="do something",
        )
        plan = WorkflowPlan(steps=[step], intent="test")

        mocker.patch.object(executor, "_execute_step", AsyncMock(return_value="done"))
        ctx = await executor.execute(plan)
        assert ctx.step_results.get("s1") == "done"

    @pytest.mark.asyncio
    async def test_execute_sequential_steps(self, mocker):
        executor = WorkflowExecutor()
        steps = [
            WorkflowStep(id="s1", name="Step 1", type=StepType.CLAUDE_SKILL),
            WorkflowStep(id="s2", name="Step 2", type=StepType.CLAUDE_SKILL, depends_on=["s1"]),
        ]
        plan = WorkflowPlan(steps=steps, intent="test")

        mocker.patch.object(executor, "_execute_step", AsyncMock(side_effect=["result1", "result2"]))
        ctx = await executor.execute(plan)
        assert ctx.step_results.get("s1") == "result1"
        assert ctx.step_results.get("s2") == "result2"

    @pytest.mark.asyncio
    async def test_execute_parallel_steps(self, mocker):
        executor = WorkflowExecutor()
        # steps with no dependency run in parallel
        steps = [
            WorkflowStep(id="s1", name="Step 1", type=StepType.CLAUDE_SKILL),
            WorkflowStep(id="s2", name="Step 2", type=StepType.CLAUDE_SKILL),
        ]
        plan = WorkflowPlan(steps=steps, max_concurrency=3, intent="test")

        mocker.patch.object(executor, "_execute_step", AsyncMock(side_effect=["a", "b"]))
        ctx = await executor.execute(plan)
        assert "s1" in ctx.step_results
        assert "s2" in ctx.step_results

    @pytest.mark.asyncio
    async def test_step_failure_marked(self, mocker):
        executor = WorkflowExecutor()
        step = WorkflowStep(id="s1", name="Failing Step", type=StepType.CLAUDE_SKILL, max_retries=0)
        plan = WorkflowPlan(steps=[step], intent="test")

        mocker.patch.object(executor, "_execute_step", AsyncMock(side_effect=RuntimeError("fail")))
        ctx = await executor.execute(plan)
        assert step.status == StepStatus.FAILED
        assert "s1" in ctx.errors
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/workflow/test_executor.py -v
```
Expected: Tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/workflow/test_executor.py
git commit -m "test: add workflow executor tests (P1)"
```

#### Task 10: test_brainstorming.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\workflow\test_brainstorming.py`

- [ ] **Step 1: Write the test**

```python
"""Test brainstorming engine — state machine, question/answer flow."""

from __future__ import annotations

from unittest.mock import MagicMock

import pytest

from app.tools.workflow.brainstorming import (
    BrainstormState,
    BrainstormingSession,
)


class TestBrainstormingSession:
    def test_initial_state(self):
        session = BrainstormingSession()
        assert session.state == BrainstormState.INITIAL
        assert len(session.clarified_dimensions) == 0

    def test_start_returns_question(self, mocker):
        session = BrainstormingSession(llm=MagicMock())
        mocker.patch.object(session, "_call_llm", return_value={
            "type": "question",
            "question": {"text": "Which platform?", "options": ["A2", "A3"], "dimension": "target_platform"},
            "assessment": "clarify_needed",
        })
        result = session.start("verify Qwen3-8B")
        assert result["type"] == "question"
        assert result["question"]["question_id"].startswith("q_")

    def test_answer_updates_dimension(self, mocker):
        session = BrainstormingSession(llm=MagicMock())
        mocker.patch.object(session, "_call_llm", return_value={
            "type": "question",
            "question": {"text": "Which model?", "options": ["Qwen3"], "dimension": "model_name"},
            "assessment": "clarify_needed",
        })
        # Start adds first question
        session.start("test")
        # Now answer with a second question
        mocker.patch.object(session, "_call_llm", return_value={
            "type": "question",
            "question": {"text": "Framework?", "options": ["vLLM"], "dimension": "inference_framework"},
            "assessment": "clarify_needed",
        })
        result = session.answer("q_1", "A2")
        assert result["type"] == "question"

    def test_answer_unknown_dimension_sets_unknown(self, mocker):
        session = BrainstormingSession(llm=MagicMock())
        mocker.patch.object(session, "_call_llm", return_value={
            "type": "proposals",
            "proposals": [{"title": "Plan A", "description": "desc"}],
            "assessment": "ready_for_proposals",
        })
        session.start("test")
        # Answer when no history dimension available
        mocker.patch.object(session, "_call_llm", return_value={"type": "proposals", "proposals": [], "assessment": "ready_for_proposals"})
        result = session.answer("q_1", "answer")
        assert result["type"] in ("proposals", "question")

    def test_approve_completes_session(self, mocker):
        session = BrainstormingSession(llm=MagicMock())
        session.state = BrainstormState.REVIEWING
        session.design_doc = MagicMock()
        session.design_doc.model_dump.return_value = {}

        result = session.approve()
        assert result["type"] == "complete"
        assert "context" in result

    def test_reject_returns_to_clarifying(self, mocker):
        session = BrainstormingSession(llm=MagicMock())
        session.state = BrainstormState.REVIEWING
        mocker.patch.object(session, "_call_llm", return_value={
            "type": "question",
            "question": {"text": "Clarify?", "options": [], "dimension": "unknown"},
            "assessment": "clarify_needed",
        })
        result = session.reject("not good")
        assert result["type"] == "question"

    def test_to_dict_and_from_dict(self, mocker):
        session = BrainstormingSession(llm=MagicMock(), session_id="test-123")
        session.clarified_dimensions["platform"] = "A2"
        data = session.to_dict()
        assert data["session_id"] == "test-123"
        assert data["clarified_dimensions"]["platform"] == "A2"

        restored = BrainstormingSession.from_dict(data)
        assert restored.session_id == "test-123"
        assert restored.clarified_dimensions["platform"] == "A2"

    def test_fallback_response_initial(self):
        session = BrainstormingSession(llm=MagicMock())
        fallback = session._fallback_response()
        assert fallback["type"] == "question"
        assert fallback["question"]["dimension"] == "target_platform"
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/workflow/test_brainstorming.py -v
```
Expected: Tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/workflow/test_brainstorming.py
git commit -m "test: add brainstorming tests (P1)"
```

---

### Phase 4: 增量加固（C1-C4）

#### Task 11: C1 — 外部调用防护

**Files:**
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\doc_tools.py` (超时常量)
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\claude_tools.py` (asyncio.wait_for)
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\workflow\mcp_integration.py` (_discover_tools 超时)
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\core\llm_factory.py` (retry 装饰器)
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\workflow\planner.py` (_call_llm 重试优先)

- [ ] **Step 1: doc_tools.py — 提取超时常量**

Read `app/tools/doc_tools.py` and replace magic numbers with named constants:
```python
# Add near top of file, after imports
_SUBPROCESS_TIMEOUT: int = 600   # generate.py 超时
_ONE_CLICK_TIMEOUT: int = 1800  # one-click 脚本超时
_MAX_OUT_LENGTH: int = 5000     # 输出截断长度
```

Then update usages: replace `timeout=600` → `timeout=_SUBPROCESS_TIMEOUT`, `timeout=1800` → `timeout=_ONE_CLICK_TIMEOUT`, `_MAX_OUT` → `_MAX_OUT_LENGTH`.

```python
# 修改后示例
completed = subprocess.run(
    cmd,
    cwd=str(td),
    capture_output=True,
    text=True,
    timeout=_SUBPROCESS_TIMEOUT,
    env=os.environ.copy(),
)
```

- [ ] **Step 2: claude_tools.py — asyncio.wait_for 兜底**

Add timeout to `_invoke_claude_skill_async`:
```python
import asyncio

# 在 _invoke_claude_skill_async 中包裹 claude_query
_CLAUDE_SKILL_TIMEOUT: int = 300

async for message in claude_query(prompt=full_prompt, options=options):
    messages.append(message)
# 改为：
try:
    async for message in asyncio.wait_for(
        claude_query(prompt=full_prompt, options=options),
        timeout=_CLAUDE_SKILL_TIMEOUT,
    ):
        messages.append(message)
except asyncio.TimeoutError:
    return f"[claude-code] 调用超时（>{_CLAUDE_SKILL_TIMEOUT}s）"
```

- [ ] **Step 3: mcp_integration.py — _discover_tools 超时**

```python
# In _discover_tools method, add timeout to send_and_receive call
if isinstance(transport, StreamableHttpTransport):
    response = await asyncio.wait_for(
        transport.send_and_receive(request),
        timeout=10,  # 10s discovery timeout
    )
```

- [ ] **Step 4: llm_factory.py — retry 装饰器**

```python
import asyncio
from functools import wraps


def retry_llm_call(max_retries: int = 3, base_delay: float = 1.0):
    """指数退避重试装饰器，用于 LLM 调用。"""
    def decorator(func):
        @wraps(func)
        async def async_wrapper(*args, **kwargs):
            last_exc = None
            for attempt in range(max_retries):
                try:
                    return await func(*args, **kwargs)
                except (Exception,) as e:
                    last_exc = e
                    if attempt < max_retries - 1:
                        delay = base_delay * (2 ** attempt)
                        await asyncio.sleep(delay)
            raise last_exc  # type: ignore[misc]

        @wraps(func)
        def sync_wrapper(*args, **kwargs):
            last_exc = None
            for attempt in range(max_retries):
                try:
                    return func(*args, **kwargs)
                except (Exception,) as e:
                    last_exc = e
                    if attempt < max_retries - 1:
                        import time
                        delay = base_delay * (2 ** attempt)
                        time.sleep(delay)
            raise last_exc  # type: ignore[misc]

        if asyncio.iscoroutinefunction(func):
            return async_wrapper
        return sync_wrapper
    return decorator
```

- [ ] **Step 5: planner.py — _call_llm 先重试再降级**

```python
# In DynamicPlanner._call_llm, add retry before fallback
from app.core.llm_factory import retry_llm_call

# Modify the method:
def _call_llm(self) -> dict:
    messages = self._build_llm_messages()
    try:
        response = retry_llm_call(max_retries=2)(self.llm.invoke)(messages)
        text = response.content if hasattr(response, "content") else str(response)
        return self._parse_json_response(text)
    except Exception as e:
        handle_api_error(e)
        return self._fallback_response()
```

- [ ] **Step 6: Run existing tests to verify no breakage**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/ -v
```
Expected: All tests still pass.

- [ ] **Step 7: Commit**

```bash
git add app/tools/doc_tools.py app/tools/claude_tools.py app/tools/workflow/mcp_integration.py app/core/llm_factory.py app/tools/workflow/planner.py
git commit -m "feat: add external call protection - timeouts and retry (C1)"
```

#### Task 12: C2 — Pydantic 校验加强

**Files:**
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\models\chat.py`

- [ ] **Step 1: Add field validators**

```python
from pydantic import BaseModel, Field, field_validator


class ChatCompletionRequest(BaseModel):
    model: str | None = None
    messages: list[ChatMessage]
    stream: bool = True
    temperature: float | None = Field(default=None, ge=0, le=2)
    max_tokens: int | None = Field(default=None, ge=1, le=131072)
    c_id: str | None = Field(default=None, max_length=128)
    confirmation: str | None = None
    brainstorm: str | None = None
    trace_id: str | None = None

    @field_validator("messages")
    @classmethod
    def validate_messages(cls, v: list[ChatMessage]) -> list[ChatMessage]:
        if not v:
            raise ValueError("messages 不能为空")
        if v[-1].role != "user":
            raise ValueError("messages 最后一条必须是 user")
        return v


class ChatCompletionChunk(BaseModel):
    id: str
    object: str = "chat.completion.chunk"
    created: int
    model: str
    choices: list[Choice]

    @field_validator("created")
    @classmethod
    def validate_timestamp(cls, v: int) -> int:
        import time
        now = int(time.time())
        if v > now + 3600:  # 不允许未来超过 1 小时的时间戳
            raise ValueError(f"created 时间戳不合理（未来 >1h）：{v}")
        return v
```

- [ ] **Step 2: Add ChatMessage.content empty handling**

```python
class ChatMessage(BaseModel):
    role: Literal["system", "user", "assistant", "tool"]
    content: str | None = None
    name: str | None = None
    tool_call_id: str | None = None

    @field_validator("content")
    @classmethod
    def empty_content_to_none(cls, v: str | None) -> str | None:
        if v is not None and v.strip() == "":
            return None
        return v
```

- [ ] **Step 3: Update tests to verify new validations**

Add to `tests/api/test_chat.py`:
```python
class TestValidationEnhancements:
    def test_temperature_clamped(self):
        req = ChatCompletionRequest(
            messages=[ChatMessage(role="user", content="hi")],
            temperature=3.0
        )
        # Field(ge=0, le=2) should clamp via Pydantic
        assert 0 <= req.temperature <= 2

    def test_max_tokens_validation(self):
        with pytest.raises(ValidationError):
            ChatCompletionRequest(
                messages=[ChatMessage(role="user", content="hi")],
                max_tokens=-1
            )

    def test_c_id_length_limit(self):
        with pytest.raises(ValidationError):
            ChatCompletionRequest(
                messages=[ChatMessage(role="user", content="hi")],
                c_id="x" * 200
            )

    def test_empty_content_becomes_none(self):
        msg = ChatMessage(role="user", content="   ")
        assert msg.content is None
```

- [ ] **Step 4: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/api/test_chat.py -v
```
Expected: All tests pass.

- [ ] **Step 5: Commit**

```bash
git add app/models/chat.py tests/api/test_chat.py
git commit -m "feat: strengthen Pydantic field validation (C2)"
```

#### Task 13: C3 — 结构化日志补全

**Files:**
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\core\agent_engine.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\workflow\planner.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\workflow\executor.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\services\chat_service.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\api\chat.py`

- [ ] **Step 1: agent_engine.py — 构建日志**

```python
# In build_agent(), after building agent:
import logging
logger = logging.getLogger(__name__)

# Add at end of build_agent:
tools_count = len(tools)
logger.info("Agent built: model=%s, tools=%d, streaming=%s", 
            llm.model, tools_count, streaming)
```

- [ ] **Step 2: planner.py — plan() 日志**

```python
# In DynamicPlanner.plan(), before returning:
import logging
logger = logging.getLogger(__name__)

# Add after plan creation:
logger.info(
    "Workflow plan created: intent=%.60s, steps=%d, goal=%.60s",
    user_input, len(plan.steps), plan.goal,
)
```

- [ ] **Step 3: executor.py — 每步日志**

```python
# In WorkflowExecutor._execute_step(), at start:
import logging
logger = logging.getLogger(__name__)

# Add at method start:
logger.info("Executing step: id=%s, name=%s, type=%s", step.id, step.name, step.type.value)

# Add on completion:
logger.info("Step completed: id=%s, status=%s", step.id, step.status.value)
```

- [ ] **Step 4: chat_service.py — 命令执行入口日志**

```python
# In _execute_command, after receiving args:
import logging
logger = logging.getLogger(__name__)

# Add after cmd lookup:
logger.info("Executing command: cmd=%s, args=%.120s, duration=%dms", cmd_name, args, ...)
```

- [ ] **Step 5: api/chat.py — 请求入口日志 + trace_id**

```python
# In chat_completions(), add entry logging:
import logging
import uuid

logger = logging.getLogger(__name__)

@router.post("/v1/chat/completions")
async def chat_completions(request: ChatCompletionRequest) -> StreamingResponse:
    trace_id = request.trace_id or uuid.uuid4().hex[:12]
    logger.info(
        "Chat request: trace_id=%s, messages=%d, stream=%s, model=%s",
        trace_id, len(request.messages), request.stream, request.model,
    )
    # ... existing code ...
```

- [ ] **Step 6: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/ -v
```
Expected: All tests pass.

- [ ] **Step 7: Commit**

```bash
git add app/core/agent_engine.py app/tools/workflow/planner.py app/tools/workflow/executor.py app/services/chat_service.py app/api/chat.py
git commit -m "feat: add structured logging to key paths (C3)"
```

#### Task 14: C4 — 错误响应统一

**Files:**
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\services\chat_service.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\core\llm_factory.py`
- Modify: `F:\7-workspace\mofix-new\mofix-v1.3\app\tools\adapt_tools.py`

- [ ] **Step 1: chat_service.py — 统一错误格式**

```python
# Replace bare error strings with structured format:
# Before:
yield _build_sse_chunk(chunk_id, model, DeltaContent(
    role="assistant", content=f"[错误] 执行失败：{e}"
))
# After:
error_msg = json.dumps({
    "error": {
        "message": str(e),
        "type": "command_error",
        "code": "execution_failed",
    }
}, ensure_ascii=False)
yield _build_sse_chunk(chunk_id, model, DeltaContent(
    role="assistant", content=error_msg
))
```

- [ ] **Step 2: adapt_tools.py — 骨架工具返回标准格式**

```python
# Replace placeholder strings:
# Before:
return f"[adapt-agent] 暂未接入，repo={repo_url_or_path!r}"
# After:
import json
return json.dumps({
    "status": "not_implemented",
    "message": f"adapt_review_repo 暂未接入（repo={repo_url_or_path!r}）",
    "tool": "adapt_review_repo",
}, ensure_ascii=False)
```

- [ ] **Step 3: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/ -v
```
Expected: All tests pass.

- [ ] **Step 4: Commit**

```bash
git add app/services/chat_service.py app/tools/adapt_tools.py
git commit -m "feat: unify error response format (C4)"
```

---

### Phase 5: P2 测试与收尾

#### Task 15: test_claude_tools.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\tools\test_claude_tools.py`

- [ ] **Step 1: Write the test**

```python
"""Test Claude tools — skill name resolution, chunking, warning filter."""

from __future__ import annotations

from app.tools.claude_tools import _chunk_text, _filter_warnings, _resolve_skill_name


class TestFilterWarnings:
    def test_removes_warning_lines(self):
        text = "INFO: line1\nWARNING: something\nINFO: line2"
        result = _filter_warnings(text)
        assert "WARNING:" not in result

    def test_compresses_multiple_newlines(self):
        text = "a\n\n\n\n\nb"
        result = _filter_warnings(text)
        assert "\n\n\n" not in result

    def test_handles_none_or_non_string(self):
        assert _filter_warnings(None) is None
        assert _filter_warnings(123) == 123

    def test_strips_trailing_newline(self):
        result = _filter_warnings("hello\n\n")
        assert not result.endswith("\n")


class TestChunkText:
    def test_short_text_no_chunking(self):
        result = _chunk_text("hello", 10)
        assert result == ["hello"]

    def test_long_line_chunked(self):
        result = _chunk_text("a" * 100, 30)
        assert len(result) > 1
        assert all(len(c) <= 30 for c in result)

    def test_empty_text(self):
        assert _chunk_text("") == []
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/tools/test_claude_tools.py -v
```

- [ ] **Step 3: Commit**

```bash
git add tests/tools/test_claude_tools.py
git commit -m "test: add Claude tools tests (P2)"
```

#### Task 16: test_doc_tools.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\tools\test_doc_tools.py`

- [ ] **Step 1: Write the test**

```python
"""Test doc-agent tools — root resolution, environment check."""

from __future__ import annotations

from unittest.mock import MagicMock, patch

import pytest


class TestDocTools:
    def test_resolve_doc_agent_root_env_var(self, mocker):
        mocker.patch("os.environ.get", return_value="/custom/doc-agent")
        from app.tools.doc_tools import resolve_doc_agent_root
        root = resolve_doc_agent_root()
        assert str(root) == "/custom/doc-agent"

    def test_check_environment_reports_issues(self, mocker):
        mocker.patch("app.tools.doc_tools.resolve_doc_agent_root", return_value=MagicMock())
        from app.tools.doc_tools import check_doc_agent_environment
        issues = check_doc_agent_environment(MagicMock())
        assert isinstance(issues, list)

    @pytest.mark.asyncio
    async def test_doc_agent_one_click_missing_env(self, mocker):
        mocker.patch("app.tools.doc_tools.resolve_doc_agent_root", side_effect=ValueError("not found"))
        from app.tools.doc_tools import doc_agent_one_click_generate
        result = doc_agent_one_click_generate()
        assert "未找到" in result or "not found" in result.lower()
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/tools/test_doc_tools.py -v
```

- [ ] **Step 3: Commit**

```bash
git add tests/tools/test_doc_tools.py
git commit -m "test: add doc tools tests (P2)"
```

#### Task 17: test_hermes.py

**Files:**
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\hermes\test_store.py`
- Create: `F:\7-workspace\mofix-new\mofix-v1.3\tests\hermes\test_memory.py`

- [ ] **Step 1: Write test_store.py**

```python
"""Test Hermes Experience Store."""

from __future__ import annotations

import json

import pytest


class TestExperienceStore:
    def test_store_initialization(self, tmp_data_dir, mocker):
        mocker.patch("app.tools.experience.store.ExperienceStore._ensure_data_dir")
        from app.tools.experience.store import ExperienceStore
        store = ExperienceStore()
        assert store is not None

    def test_store_empty_memories(self, tmp_data_dir, mocker):
        mocker.patch("app.tools.experience.store.ExperienceStore._ensure_data_dir")
        mocker.patch("app.tools.experience.store.ExperienceStore.count_memories", return_value=0)
        from app.tools.experience.store import ExperienceStore
        store = ExperienceStore()
        assert store.count_memories() == 0

    def test_store_pending_insights_empty(self, tmp_data_dir, mocker):
        mocker.patch("app.tools.experience.store.ExperienceStore._ensure_data_dir")
        mocker.patch("app.tools.experience.store.ExperienceStore.get_pending_insights", return_value=[])
        from app.tools.experience.store import ExperienceStore
        store = ExperienceStore()
        assert store.get_pending_insights() == []
```

- [ ] **Step 2: Run tests**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/hermes/ -v
```

- [ ] **Step 3: Commit**

```bash
git add tests/hermes/
git commit -m "test: add Hermes engine tests (P2)"
```

#### Task 18: 全局 coverage 审计与补漏

- [ ] **Step 1: Run full coverage report**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/ --cov=app --cov-report=term-missing --cov-report=html
```

- [ ] **Step 2: 识别覆盖率缺口，补充关键测试**

Check the HTML report at `F:\7-workspace\mofix-new\mofix-v1.3\htmlcov\index.html`. Focus on modules with < 60% coverage.

- [ ] **Step 3: 运行全部测试验证无回归**

```bash
cd F:\7-workspace\mofix-new\mofix-v1.3
pytest tests/ -v --tb=short
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "test: final coverage audit and gap filling"
```

---

## Spec 对照清单

| Spec 章节 | 对应 Task |
|-----------|-----------|
| 目录结构 | Task 1 |
| pytest.ini + requirements-dev.txt | Task 1 |
| tests/conftest.py | Task 1 |
| .github/workflows/ci.yml | Task 1 |
| P0: test_exceptions.py | Task 2 |
| P0: test_llm_factory.py | Task 3 |
| P0: test_health.py + test_chat.py | Task 4 |
| P0: test_commands.py | Task 5 |
| P1: test_agent_engine.py | Task 6 |
| P1: test_chat_service.py | Task 7 |
| P1: test_planner.py | Task 8 |
| P1: test_executor.py | Task 9 |
| P1: test_brainstorming.py | Task 10 |
| C1: 外部调用防护 | Task 11 |
| C2: Pydantic 校验加强 | Task 12 |
| C3: 结构化日志补全 | Task 13 |
| C4: 错误响应统一 | Task 14 |
| P2: test_claude_tools.py | Task 15 |
| P2: test_doc_tools.py | Task 16 |
| P2: test_hermes | Task 17 |
| 收尾 coverage 审计 | Task 18 |
