[pytest]
testpaths = tests
markers =
    slow: tests requiring real LLM or external dependencies
    integration: integration tests needing external services
    isolated: pure unit tests (default, can run offline)
pythonpath = .
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
