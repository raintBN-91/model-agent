[
    {
        "id": 1,
        "prompt": "部署 xception71.tf_in1k 模型到 NPU 并执行推理验证精度",
        "expected": "完成 CPU/NPU 推理、精度对比、误差 < 1%，输出推理结果和对比报告"
    },
    {
        "id": 2,
        "prompt": "串行执行全部 6 个 Xception 模型（xception71、xception65p、xception65、xception41p、xception41）的 NPU 推理和精度验证",
        "expected": "六个模型依次串行执行，每个完成后释放资源，精度均 < 1%"
    },
    {
        "id": 3,
        "prompt": "NPU 不可用时回退到 CPU 完成 xception41.tf_in1k 推理",
        "expected": "检测到 NPU 不可用后自动回退 CPU，输出推理结果并标记 fallback"
    }
]
